# Optional ML/NLP extras — heavyweight dependencies the platform can use
# but never requires (every feature has an offline fallback engine).
#
#   pip install -r requirements-ml.txt
#   python -m spacy download en_core_web_sm
#
# Enable via env vars: NLP_ENGINE=spacy|transformers, DLP_ENGINE=presidio.

# NLP pipeline (NER via spaCy; zero-shot classification via HF Transformers)
spacy>=3.7
transformers>=4.44
torch>=2.3

# PII redaction (Microsoft Presidio)
presidio-analyzer>=2.2
presidio-anonymizer>=2.2

# LLM-judged RAG evaluation (optional deep pass; custom metrics are built in)
ragas>=0.2
